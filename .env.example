# Port the server listens on (Render sets this automatically)
PORT=8000

# Your Vercel frontend URL — set this in Render's environment variables
# so the CORS policy allows requests from your deployed frontend.
# Example: https://image-frontend-black.vercel.app
FRONTEND_URL=https://image-frontend-black.vercel.app
